{
  "name": "sandbox",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@types/node": "*",
    "nodemon": "*",
    "ts-node": "*",
    "tsconfig-paths": "*",
    "typescript": "*"
  },
  "scripts": {
    "clean": "npx rimraf yarn.lock node_modules dist",
    "setup": "yarn clean && yarn install --ignore-optional"
  },
  "author": "lulzneko",
  "license": "MIT"
}
